/**
 * Funciones del frontend llamadas
 */
/*const { ipcRenderer } = require("electron");
const { CHANNELS } = require("../../shared/channels");

module.exports = {
  list(query) {
    return ipcRenderer.invoke(CHANNELS.DIRECTORY.LIST, query);
  },

  create(data) {
    return ipcRenderer.invoke(CHANNELS.DIRECTORY.CREATE, data);
  },

  update(id, data) {
    return ipcRenderer.invoke(CHANNELS.DIRECTORY.UPDATE, { id, data });
  },
};
*/